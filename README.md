# Pizzería El Atraco – Proyecto Web

Este proyecto utiliza TailwindCSS en modo local para estilos.  
A continuación se explica cómo instalar dependencias y ejecutar Tailwind en modo desarrollo.

---

## 🚀 Requisitos
- Node.js instalado (versión 14 o superior)
- NPM incluido con Node.js
- Solo funciona en local

---

## 📦 Instalación

1. Inicializar el proyecto (si no existe `package.json`):
   ```bash
   npm init -y
2 Compilar Tailwind
   ```bash
    npx tailwindcss -i ./src/input.css -o ./src/output.css --watch



